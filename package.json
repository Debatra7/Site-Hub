{
  "name": "beta-hub",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev": "npm run dev --workspace=client",
    "client:build": "npm run build --workspace=client",
    "server:dev": "npm run dev --workspace=client",
    "build": "npm run build --workspace=client"
  }
}
